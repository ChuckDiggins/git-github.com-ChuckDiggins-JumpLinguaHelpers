import XCTest
@testable import JumpLinguaHelpers

final class JumpLinguaHelpersTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(JumpLinguaHelpers().text, "Hello, World!")
    }
}
